#include<stdio.h>
void main(){
    int bin[32],n,i,rem,a[32],r;
    printf("Enter an integer: ");
    scanf("%d",&n);
    if(n>=0)
    {
    	for(i=0;i<=31;i++)
		{
        bin[i]=0;
    	}
    	for(i=0;n!=0;i++)
		{
        	rem=n%2;
        	bin[i]=rem;
        	n=n/2;
    	}
    	for(i=31;i>=0;i--)
		{
        	printf("%d",bin[i]);
    	}
	}
	else
	{
		n=(-n);
		for(i=0;i<=31;i++)
		{
        	bin[i]=0;
    	}
    	for(i=0;n!=0;i++)
		{
        	rem=n%2;
        	bin[i]=rem;
        	n=n/2;
    	}
    	for(i=31;i>=0;i--)
		{
        	a[31-i]=bin[i];
    	}
    	for(i=0;i<=31;i++)
    	{
    		if(a[i]==0)
    		{
    			a[i]=1;
			}
			else
			{
				a[i]=0;
			}
		}
		if(a[31]==0)
		{
			a[31]=1;
			for(i=0;i<=31;i++)
			{
        		printf("%d",a[i]);
    		}
		}
		else
		{
			a[31]=0;
			for(i=30;i>=0;i--)
			{
        		if(a[i]==1)
        		{
        			a[i]=0;
				}
				else
				{
					a[i]=1;
					break;
				}
    		}
    		for(i=0;i<=31;i++)
			{
        		printf("%d",a[i]);
    		}
		}
	}
}
